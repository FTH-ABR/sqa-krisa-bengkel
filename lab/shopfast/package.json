{
  "name": "shopfast",
  "version": "1.0.0",
  "description": "ShopFast synthetic checkout system for the SQA Bengkel training lab (fictitious data).",
  "private": true,
  "main": "app/server.js",
  "scripts": {
    "start": "node app/server.js",
    "test:api": "newman run tests/api/shopfast.postman_collection.json -e tests/api/local.postman_environment.json",
    "test:ui": "playwright test",
    "test": "node tests/run-all.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "newman": "^6.2.2"
  }
}
